package com.github.matheusvizzaccaro.mycalc.controller;

public class LoginController {
  Boolean isValid(String username, String password) {
    //needs to call model and then validate the return
  }
}
